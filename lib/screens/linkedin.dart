import 'package:flutter/material.dart';

class CareerInScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: Implement your Career screen UI here
    return Scaffold(
      appBar: AppBar(
        title: Text('Career'),
      ),
      body: Center(
        child: Text('Career Screen'),
      ),
    );
  }
}
